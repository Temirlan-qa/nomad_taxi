enum TextFieldFocusState { focused, unfocused }

enum TextFieldValidationState { success, error, none }

enum OrderStateEnum {
  searching,
  accepted,
  progress,
  waiting,
  completed,
  canceled,
  error,
}
